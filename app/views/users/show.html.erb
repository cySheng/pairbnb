<h2><%= current_user.last_name%> Profile</h2>
<%= link_to t(".edit_profile"), edit_user_path %> <br>

<%= image_tag(current_user.avatar_url) if current_user.avatar? %>


<h4> My Listings</h4>
<% current_user.listings.each do |x| %>
	<%= link_to (x.name), edit_listing_path(x) %>
<% end %>
