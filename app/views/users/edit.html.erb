<div id="clearance" class="update">
  <h2><%= t(".title") %></h2>
  <% byebug %>
  <%= form_for @user do |form| %>
    <%= render partial: '/users/fbform', object: form %>

    <div class="submit-field">
      <%= form.submit %>
    </div>

  <% end %>
</div>
